export interface Product {
  userId: string,
  productId: string,
  productName: string
  description: string,
  size: string,
  price: number,
  sold: boolean,
  createdAt: string,
  attachedImageUrl: string
}